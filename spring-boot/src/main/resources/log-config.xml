<?xml version="1.0" encoding="UTF-8"?>
<included>
    <property name="log.path" value="log"/>
    <springProperty name="log.name" scope="context" source="hztour.logfile.name" defaultValue="service" />
    <property name="CONSOLE_LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss} [%thread] %cyan([%-35.36logger-%-5level]) %green([%-36.36class]) > %msg %n"/>

    <!--输出到控制台-->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!--输出到文件-->
    <appender name="file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${log.path}/log_${log.name}_%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %contextName [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!--输出错误日志到文件-->
    <appender name="error_file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${log.path}/log_${log.name}_error_%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %contextName [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter"><!-- 只打印ERROR日志 -->
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <!-- IDE环境 -->
    <springProfile name="local">
        <root level="info">
            <appender-ref ref="console"/>
            <appender-ref ref="file"/>
            <appender-ref ref="error_file"/>
        </root>

    </springProfile>


    <!-- 测试环境 -->
    <springProfile name="dev">
        <root level="info">
            <appender-ref ref="file"/>
            <appender-ref ref="error_file"/>
        </root>

    </springProfile>

    <!-- 预发环境. -->
    <springProfile name="pre">
        <root level="info">
            <appender-ref ref="file"/>
            <appender-ref ref="error_file"/>
        </root>

    </springProfile>

    <!-- 生产环境. -->
    <springProfile name="prod">
        <root level="error">
            <appender-ref ref="file"/>
            <appender-ref ref="error_file"/>
        </root>
    </springProfile>


</included>